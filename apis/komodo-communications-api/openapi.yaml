openapi: "3.1.0"
info:
  title: komodo-communications-api
  version: "0.1.0"
  description: |
    Communications service for sending notifications, emails, and messages.
servers:
  - url: http://localhost:7032
    description: Local dev

tags:
  - name: Health

paths:
  /health:
    get:
      tags: [Health]
      summary: Liveness check
      operationId: healthCheck
      security: []
      responses:
        "200":
          description: Service is healthy

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
