// E2E tests — require a running instance (LocalStack + real service).
// Run with: BASE_URL=http://localhost:7044 cargo test --test e2e
//
// These are intentionally empty until the service is implemented.
