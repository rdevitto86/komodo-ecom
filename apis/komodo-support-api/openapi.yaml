openapi: "3.1.0"
info:
  title: komodo-support-api
  version: "0.1.0"
  description: |
    Customer support service with AI-powered chat. Supports both anonymous (cookie session)
    and authenticated (JWT) users. Currently backed by in-memory storage — wire DynamoDB before prod.

    HTTP conventions: POST → 201 Created / 400 Bad Request / 409 Conflict;
    PUT → 200 OK / 404 Not Found; DELETE → 204 No Content / 404 Not Found;
    GET → 200 OK / 404 Not Found.

servers:
  - url: http://localhost:7101
    description: Local dev

tags:
  - name: Health
  - name: Session
  - name: Chat

paths:
  /health:
    get:
      tags: [Health]
      summary: Liveness check
      operationId: healthCheck
      security: []
      responses:
        "200":
          description: Service is healthy

  /v1/chat/session:
    post:
      tags: [Session]
      summary: Create a new chat session
      operationId: createSession
      x-visibility: public
      security: []
      responses:
        "201":
          description: Session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
    get:
      tags: [Session]
      summary: Get current chat session
      operationId: getSession
      x-visibility: public
      security: []
      responses:
        "200":
          description: Session details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"

  /v1/chat/message:
    post:
      tags: [Chat]
      summary: Send a message to the AI support agent
      operationId: sendMessage
      x-visibility: public
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageRequest"
      responses:
        "200":
          description: AI response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /v1/chat/history:
    get:
      tags: [Chat]
      summary: Get chat history for a session
      operationId: getHistory
      x-visibility: public
      security: []
      parameters:
        - name: session
          in: query
          required: true
          description: |
            Session ID to fetch history for. [TODO] wildcard fetch (omit param) to return all
            messages for the authenticated user is not yet implemented.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Chat history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatHistory"
    delete:
      tags: [Chat]
      summary: Delete chat history for current session
      operationId: deleteHistory
      x-visibility: public
      security: []
      responses:
        "204":
          description: History deleted

  /v1/chat/escalate:
    post:
      tags: [Chat]
      summary: Escalate to human support
      operationId: escalate
      x-visibility: public
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EscalateRequest"
      responses:
        "200":
          description: Escalation request created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationResponse"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Session:
      type: object
      required: [session_id, created_at]
      properties:
        session_id:
          type: string
          format: uuid
        user_id:
          type: string
          description: Present for authenticated sessions
        created_at:
          type: string
          format: date-time

    MessageRequest:
      type: object
      required: [message]
      properties:
        message:
          type: string

    MessageResponse:
      type: object
      required: [message, role, timestamp]
      properties:
        message:
          type: string
        role:
          type: string
          enum: [assistant, user]
        timestamp:
          type: string
          format: date-time

    ChatMessage:
      type: object
      required: [message, role, timestamp]
      properties:
        message:
          type: string
        role:
          type: string
          enum: [assistant, user]
        timestamp:
          type: string
          format: date-time

    ChatHistory:
      type: object
      required: [messages]
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ChatMessage"

    EscalateRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
        contact_info:
          type: string

    EscalationResponse:
      type: object
      required: [ticket_id, status]
      properties:
        ticket_id:
          type: string
        status:
          type: string
          enum: [pending, assigned, resolved]

    Problem:
      type: object
      description: RFC 7807 Problem+JSON
      required: [type, title, status]
      properties:
        type:
          type: string
          format: uri
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        instance:
          type: string

  responses:
    BadRequest:
      description: Invalid request body or parameters
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    Unauthorized:
      description: Missing or invalid JWT
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
