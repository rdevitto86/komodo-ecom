#[tokio::test]
#[ignore = "requires spawn_app to be implemented"]
async fn health_returns_ok() {
    // let addr = common::spawn_app().await;
    // let res = reqwest::get(format!("{}/health", addr)).await.unwrap();
    // assert_eq!(res.status(), 200);
    todo!()
}