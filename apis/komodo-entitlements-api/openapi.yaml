openapi: "3.1.0"
info:
  title: komodo-entitlements-api
  version: "0.1.0"
  description: |
    Entitlements service. Currently stubbed - only /health endpoint available.
servers:
  - url: http://localhost:7038
    description: Local dev

tags:
  - name: Health

paths:
  /health:
    get:
      tags: [Health]
      summary: Liveness check
      operationId: healthCheck
      security: []
      responses:
        "200":
          description: Service is healthy

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT